rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 같은 Google 계정으로 로그인한 본인의 기록만 읽고 쓸 수 있음
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null
                         && request.auth.uid == userId;
    }
  }
}
